import Header from "../../components/Header/Header";import Footer from "../../components/Footer/Footer";import PersonCard from "../../components/Personcard/PersonCard";import DotLoader from "react-spinners/DotLoader";import{useState,useEffect}from "react";import{calculateYearsInService,schedulReminder,animalToEmoji}from "./PersonListFunctions";import GetDepartmentImage from "../../components/Employees/DepartmentImages";import "./loading.css";const PersonList=({employeeData,onEdit})=>{const[loading,setLoading]=useState(!0);useEffect(()=>{const timer=setTimeout(()=>setLoading(!1),3000);return()=>clearTimeout(timer)},[]);return(<div><div className={loading?"pageLoading":""}></div>{loading&&(<div className="spinner"><DotLoader color="#FFFFFF" loading={!0}size={200}/><p className="loadingText">Loading,please wait...</p></div>)}
{!loading&&(<div><Header appName="Hr App"/><h2>Employee list</h2><main><div className="employeeTickets">{employeeData.map((employee)=>{const yearsInService=calculateYearsInService(employee);const reminderEmoji=schedulReminder(yearsInService);const animalEmoji=animalToEmoji(employee.animal);const departmentImage=GetDepartmentImage(employee.department);return(<div key={employee.id}className="message"><PersonCard{...employee}reminderEmoji={reminderEmoji}animalEmoji={animalEmoji}yearsInService={yearsInService}departmentImage={departmentImage}onEdit={onEdit}/></div>)})}</div></main><Footer className="REACT25"/></div>)}</div>)};export default PersonList